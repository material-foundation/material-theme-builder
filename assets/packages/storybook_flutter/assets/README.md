At the moment for having the required files generated for font processing the `pubspec.yaml` should have an entry in the
assets section.

Since git doesn't allow checking in empty folders, we have this file here.
